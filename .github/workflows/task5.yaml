on:[push]

jobs:
  node:
    runs-on: ubuntu-latest
    steps:
      - name: "checkout"
        uses: actions/checkout@v4
      - name: "setup of node"
        uses: actions/setup-node@v4
        with:
          node-version: 18
        - run: npm install
        - run: npm test